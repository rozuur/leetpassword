function generatePasswords(){
    var hint = document.getElementsByName('hint')[0].value;
    var passwds = leetPasswords(hint);
    alert(passwds[0] + "|" + passwds[1]);
}
                                                 

function leetPasswords(hint){
    
}
